DATABASE_URL="postgresql://user:password@localhost:5432/flatcart"
# For Neon: DATABASE_URL is the pooled connection string, DIRECT_URL is the
# same host with "-pooler" removed (used for running migrations).
DIRECT_URL="postgresql://user:password@localhost:5432/flatcart"
JWT_SECRET="change-this-to-a-long-random-string"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
